<div id="people">
    {{#each .}}
        <div id="{{type}}" class="people-group {{type}}">
          <div class="group-heading">Group heading here</div>
            {{> person_group .}}
        </div>
    {{/each}}
</div>
